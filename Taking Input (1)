#include<stdio.h>

int main () {

    int a ;

    printf("Enter any integer") ;
    scanf("%d" , &a) ;                            // this scanf function is used to scan the input from the user with help of format specifier according to the datatype .
    printf("Given Integer is %d" , a ) ;    
}
